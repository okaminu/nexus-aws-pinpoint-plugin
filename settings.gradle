rootProject.name = 'nexus-aws-pinpoint-plugin'
gradle.ext.group = 'lt.boldadmin.nexus'
gradle.ext.version = '1.3.0'

String[] includePaths = ['../nexus-api']

for(String path: includePaths)
    if( file(path).exists())
        includeBuild path